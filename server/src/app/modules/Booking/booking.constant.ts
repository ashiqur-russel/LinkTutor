export enum BookingStatus {
  PENDING = "pending",
  CONFIRMED = "confirmed",
  COMPLETED = "completed",
  CANCELED = "canceled",
}

export enum CanceledBy {
  STUDENT = "student",
  TUTOR = "tutor",
  NONE = "none",
}
